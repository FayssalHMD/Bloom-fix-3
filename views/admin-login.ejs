<!DOCTYPE html>
<html lang="fr" class="">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - Bloom</title>
    <link rel="icon" type="image/png" href="/images/title-icon.png">
    <link rel="stylesheet" href="/styles/style.css">
    <link rel="stylesheet" href="/styles/admin.css">
</head>
<body>
    <%- include('partials/admin-header') %>
    <main class="admin-login-container">
        <div class="login-box">
            <!-- CHANGED: Added the logo -->
            <img src="/images/flower_loading.png" alt="Bloom Logo" class="login-logo">
            
            <h1>Accès Administrateur</h1>
            <p class="subtitle">Veuillez entrer le mot de passe pour continuer.</p>

            <% if (error) { %>
                <p class="error-message"><%= error %></p>
            <% } %>

            <form action="/admin/login" method="POST">
            
                <div class="form-group">
                    <input type="password" name="password" placeholder="Mot de passe" required>
                </div>
                <button type="submit" class="admin-btn admin-btn-primary btn-submit">Se connecter</button>
            </form>
        </div>
    </main>
    <%- include('partials/footer') %>
  
</body>
</html>