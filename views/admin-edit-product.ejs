<!DOCTYPE html>
<html lang="fr" class="">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier: <%= product.name.fr %> - Bloom Admin</title>
    <link rel="icon" type="image/png" href="/images/title-icon.webp">
    <link rel="stylesheet" href="/styles/style.css">
    <link rel="stylesheet" href="/styles/admin.css">
    <link rel="stylesheet" href="/styles/admin-form.css">
</head>
<body>
    <%- include('partials/admin-header') %>

    <main class="admin-main">
        <div class="composer-header">
            <h1>Modifier le Produit</h1>
            <a href="/admin/products" class="btn-cancel">← Annuler</a>
        </div>

        <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
            <div class="admin-error-banner">
                <strong>Erreur de validation :</strong>
                <ul>
                    <% errors.forEach(error => { %>
                        <li><%= error.msg %></li>
                    <% }) %>
                </ul>
            </div>
        <% } %>
        
        <form action="/admin/products/edit/<%= product._id %>" method="POST" enctype="multipart/form-data">
            
            <div class="composer-main">
                <!-- Left Panel: Main Info -->
                <div class="composer-panel composer-panel-main">
                    
                    <!-- ================================================== -->
                    <!--                 START OF I18N CHANGES                -->
                    <!-- ================================================== -->
                    <div class="lang-tabs">
                        <div class="tab active" data-lang="fr">Français</div>
                        <div class="tab" data-lang="ar">العربية</div>
                    </div>

                    <!-- French Panel -->
                    <div id="panel-fr" class="lang-panel active">
                        <h2>Informations Essentielles (FR)</h2>
                        <p class="subtitle">Version française du contenu.</p>
                        
                        <div class="form-group">
                            <label for="name-fr">Nom du Produit</label>
                            <input type="text" id="name-fr" name="name[fr]" value="<%= product.name.fr %>" required>
                        </div>
                        <div class="form-group">
                            <label for="short_description-fr">Description Courte</label>
                            <textarea id="short_description-fr" name="short_description[fr]" rows="3" required><%= product.short_description.fr %></textarea>
                        </div>
                        <div class="form-group">
                            <label for="description-fr">Description Complète</label>
                            <textarea id="description-fr" name="description[fr]" rows="6" required><%= product.description.fr %></textarea>
                        </div>
                    </div>

                    <!-- Arabic Panel -->
                    <div id="panel-ar" class="lang-panel">
                        <h2>المعلومات الأساسية (AR)</h2>
                        <p class="subtitle">النسخة العربية للمحتوى.</p>
                        
                        <div class="form-group">
                            <label for="name-ar">اسم المنتج</label>
                            <input type="text" id="name-ar" name="name[ar]" value="<%= product.name.ar %>" required dir="rtl">
                        </div>
                        <div class="form-group">
                            <label for="short_description-ar">وصف قصير</label>
                            <textarea id="short_description-ar" name="short_description[ar]" rows="3" required dir="rtl"><%= product.short_description.ar %></textarea>
                        </div>
                        <div class="form-group">
                            <label for="description-ar">وصف كامل</label>
                            <textarea id="description-ar" name="description[ar]" rows="6" required dir="rtl"><%= product.description.ar %></textarea>
                        </div>
                    </div>
                    <!-- ================================================== -->
                    <!--                  END OF I18N CHANGES                 -->
                    <!-- ================================================== -->
                </div>

                <!-- Right Panel: Media & Attributes -->
                <div class="composer-panel composer-panel-side">
                    <div class="form-group">
                        <label for="id">ID Produit (slug)</label>
                        <input type="text" id="id" name="id" value="<%= product.id %>" readonly>
                    </div>
                    <div class="form-group">
                        <label for="price">Prix (DA)</label>
                        <input type="number" id="price" name="price" value="<%= product.price %>" step="0.01" required>
                    </div>
                    <hr class="form-divider">
                    <div class="form-group">
                        <label for="mainImage">Changer l'Image Principale</label>
                        <label for="mainImage" class="image-upload-zone">
                            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="17 8 12 3 7 8"></polyline><line x1="12" y1="3" x2="12" y2="15"></line></svg>
                            <p>Glissez-déposez ou cliquez</p>
                            <span>Pour remplacer l'image actuelle</span>
                        </label>
                        <input type="file" id="mainImage" name="mainImage" accept="image/*" style="display: none;">
                        <div id="main-image-preview" class="image-previews">
                            <% if (product.mainImage && product.mainImage.url) { %>
                                <div class="img-preview">
                                    <img src="<%= product.mainImage.url %>" alt="Image principale actuelle">
                                </div>
                            <% } %>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="gallery">Gérer les Images de la Galerie</label>
                        <label for="gallery" class="image-upload-zone">
                             <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="17 8 12 3 7 8"></polyline><line x1="12" y1="3" x2="12" y2="15"></line></svg>
                            <p>Ajouter de nouvelles images</p>
                        </label>
                        <input type="file" id="gallery" name="gallery" accept="image/*" style="display: none;" multiple>
                        <div id="gallery-previews" class="image-previews">
                            <% if (product.gallery && product.gallery.length > 0) { %>
                                <% product.gallery.forEach(image => { %>
                                    <div class="img-preview" data-public-id="<%= image.public_id %>">
                                        <img src="<%= image.url %>" alt="Image de la galerie">
                                        <button type="button" class="remove-img-btn" aria-label="Supprimer l'image">×</button>
                                    </div>
                                <% }) %>
                            <% } %>
                        </div>
                        <input type="hidden" id="images_to_delete" name="images_to_delete" value="">
                    </div>
                    <hr class="form-divider">

                    <!-- ================================================== -->
                    <!--                 START OF I18N CHANGES                -->
                    <!-- ================================================== -->
                    <!-- French Panel (Side) -->
                    <div id="panel-fr-side" class="lang-panel active">
                        <div class="form-group" id="ingredients-tag-input-fr">
                            <label for="ingredients-input-fr">Ingrédients (FR)</label>
                            <div class="tag-input-container">
                                <div class="tags"></div>
                                <input type="text" id="ingredients-input-fr" placeholder="Ajouter et presser Entrée...">
                                <input type="hidden" name="ingredients[fr]" value="<%= product.ingredients.fr.join(',') %>">
                            </div>
                        </div>

                        <div class="form-group" id="how-to-use-tag-input-fr">
                            <label for="how-to-use-input-fr">Conseils d'utilisation (FR)</label>
                            <div class="tag-input-container">
                                <div class="tags"></div>
                                <input type="text" id="how-to-use-input-fr" placeholder="Ajouter et presser Entrée...">
                                <input type="hidden" name="how_to_use[fr]" value="<%= product.how_to_use.fr.join(',') %>">
                            </div>
                        </div>
                    </div>

                    <!-- Arabic Panel (Side) -->
                    <div id="panel-ar-side" class="lang-panel">
                        <div class="form-group" id="ingredients-tag-input-ar">
                            <label for="ingredients-input-ar">المكونات (AR)</label>
                            <div class="tag-input-container">
                                <div class="tags"></div>
                                <input type="text" id="ingredients-input-ar" placeholder="أضف واضغط على إدخال..." dir="rtl">
                                <input type="hidden" name="ingredients[ar]" value="<%= product.ingredients.ar.join(',') %>">
                            </div>
                        </div>

                        <div class="form-group" id="how-to-use-tag-input-ar">
                            <label for="how-to-use-input-ar">نصائح الاستخدام (AR)</label>
                            <div class="tag-input-container">
                                <div class="tags"></div>
                                <input type="text" id="how-to-use-input-ar" placeholder="أضف واضغط على إدخال..." dir="rtl">
                                <input type="hidden" name="how_to_use[ar]" value="<%= product.how_to_use.ar.join(',') %>">
                            </div>
                        </div>
                    </div>
                    <!-- ================================================== -->
                    <!--                  END OF I18N CHANGES                 -->
                    <!-- ================================================== -->
                </div>
            </div>

            <div class="composer-actions">
                <a href="/admin/products" class="btn-cancel">Annuler</a>
                <button type="submit" class="admin-btn admin-btn-primary btn-submit">Mettre à jour le Produit</button>
            </div>
        </form>
    </main>

    <%- include('partials/footer') %>
    <script nonce="<%= nonce %>" src="/functions/admin.js"></script>
    <script nonce="<%= nonce %>" src="/functions/admin-form.js"></script>
</body>
</html>