<!DOCTYPE html>
<!-- ================= START OF I18N CHANGES ================ -->
<html lang="<%= lng %>" dir="<%= lng === 'ar' ? 'rtl' : 'ltr' %>">
<!-- ================= END OF I18N CHANGES ================== -->
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- FIX: Use language-specific pack name -->
    <title><%= pageTitle %></title>
    <meta name="description" content="<%= metaDescription %>">

    <!-- ================================================== -->
    <!--     START: ADD AMIRI & CAIRO FOR ARABIC FONTS    -->
    <!-- ================================================== -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Amiri:wght@700&family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <!-- ================================================== -->
    <!--      END: ADD AMIRI & CAIRO FOR ARABIC FONTS     -->
    <!-- ================================================== -->
     
    <link rel="icon" type="image/png" href="/images/title-icon.webp">
    <link rel="stylesheet" href="/styles/style.css">
    <link rel="stylesheet" href="/styles/products.css">
    
</head>
<body class="no-scroll">

    <!-- FIX: Pass i18n variables to header -->
    <%- include('partials/header', { t: t, lng: lng, pageType: pageType, currentUrl: currentUrl, user: user }) %>

    <main class="product-page-main">
    <section class="product-detail-section">
        <% 
            const getImageUrl = (image) => image && image.url ? image.url : image;
            const primaryImage = getImageUrl(pack.mainImage) || '/images/placeholder.png';
        %>

        <div class="product-detail-layout">
            <div class="product-image-gallery">
                <div class="image-bg-shape"></div>
                <!-- FIX: Use language-specific alt text -->
                <img src="<%= primaryImage %>" alt="<%= pack.name[lng] || pack.name.fr %>" id="main-product-image" class="main-product-image">
                
                <div class="product-thumbnail-container" id="product-thumbnail-container">
                    <img src="<%= primaryImage %>" alt="Thumbnail for <%= pack.name[lng] || pack.name.fr %>" class="thumbnail-image active" data-src="<%= primaryImage %>">
                    <% if (pack.gallery && pack.gallery.length > 0) { %>
                        <% pack.gallery.forEach((galleryImage, index) => { %>
                            <% const imageUrl = getImageUrl(galleryImage); %>
                            <img src="<%= imageUrl %>" alt="Thumbnail <%= index + 2 %> for <%= pack.name[lng] || pack.name.fr %>" class="thumbnail-image" data-src="<%= imageUrl %>">
                        <% }) %>
                    <% } %>
                </div>
            </div>
            <div class="product-purchase-panel">
                <!-- FIX: Display language-specific name and contents -->
                <h1 id="pack-name"><%= pack.name[lng] || pack.name.fr %></h1>
                <p id="pack-contents"><%= pack.contents[lng] || pack.contents.fr %></p>

                <div class="quantity-section">
                    <div class="quantity-selector">
                        <button id="quantity-minus" class="quantity-btn" aria-label="Réduire la quantité">-</button>
                        <span id="quantity-display" class="quantity-value">1</span>
                        <button id="quantity-plus" class="quantity-btn" aria-label="Augmenter la quantité">+</button>
                    </div>
                    <div class="price-container pack-price-container">
                        <span class="original-price-detail"><%= pack.originalPrice %> DA</span>
                        <span id="pack-price" data-base-price="<%= pack.discountedPrice %>"><%= parseFloat(pack.discountedPrice).toFixed(2).replace('.', ',') %> DA</span>
                    </div>
                </div>
                
                <div class="action-buttons">
                   <button id="add-to-cart-btn" 
                            class="btn-product-primary" 
                            data-product-id="<%= pack._id %>"
                            data-product-name="<%= pack.name[lng] || pack.name.fr %>"
                            data-product-price="<%= pack.discountedPrice %>"
                            data-product-image="<%= primaryImage %>"
                            data-is-pack="true">
                        <!-- FIX: Translate button text -->
                        <%= t('buttons.addToCart') %>
                    </button>
                    <button id="buy-now-btn" 
                            class="btn-product-secondary"
                            data-item-id="<%= pack._id %>"
                            data-item-name="<%= pack.name[lng] || pack.name.fr %>"
                            data-item-price="<%= pack.discountedPrice %>"
                            data-item-image="<%= primaryImage %>"
                            data-is-pack="true">
                        <!-- FIX: Translate button text -->
                        <%= t('buttons.buyNow') %>
                    </button>
                </div>
            </div>
        </div>
    </section>

    <section class="product-info-tabs">
        <div class="tab-nav">
            <!-- FIX: Translate tab title -->
            <button class="tab-link active" data-tab="description"><%= t('product.description') %></button>
        </div>
        <div class="tab-content-container">
            <div id="tab-description" class="tab-panel active">
                <!-- FIX: Display language-specific description -->
                <p><%- (pack.description[lng] || pack.description.fr).replace(/\n/g, '<br>') %></p>
            </div>
        </div>
    </section>
</main>

    <%- include('partials/footer') %>
    <%- include('partials/modals') %>  
</body>
</html>